name: "Hello Actions"
description: "Greet someone"
author: "someone@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "Gauron dorian"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
